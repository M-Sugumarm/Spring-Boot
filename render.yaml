services:
  - type: web
    name: megtodo
    env: java
    buildCommand: mvn clean package
    startCommand: java -jar target/megtodo-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
